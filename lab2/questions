questions

q1. by using socket().socket() and specifying the socket type as SOCK_STREAM, the default protocol that's used is TCP
q2. server has to listen to incoming requests, receives data before sends. the client does not listen, just sends data then wait to receive a reponse. 
